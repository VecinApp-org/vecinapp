import 'package:flutter/material.dart';

class EventCardFooter extends StatelessWidget {
  const EventCardFooter({super.key});

  @override
  Widget build(BuildContext context) {
    return Row(children: [
      IconButton(
        onPressed: () {},
        icon: const Icon(Icons.emoji_emotions),
      ),
    ]);
  }
}
