/// A placeholder class that represents an entity or model.
class Rulebook {
  const Rulebook(this.id, this.title, this.description);

  final int id;
  final String title;
  final String description;
}
