import 'package:flutter/material.dart';

class RegisterCloudUserView extends StatelessWidget {
  const RegisterCloudUserView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('RegisterCloudUserView'),
      ),
    );
  }
}
