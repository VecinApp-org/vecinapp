import 'package:flutter/material.dart';

class NoNeighborhoodView extends StatelessWidget {
  const NoNeighborhoodView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('NoNeighborhoodView'),
      ),
    );
  }
}
