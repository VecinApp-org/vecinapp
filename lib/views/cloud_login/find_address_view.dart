import 'package:flutter/material.dart';

class FindAddressView extends StatelessWidget {
  const FindAddressView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('FindAddressView'),
      ),
    );
  }
}
